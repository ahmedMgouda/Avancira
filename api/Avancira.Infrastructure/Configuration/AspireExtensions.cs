namespace Avancira.Infrastructure.Configuration;

public static class AspireExtensions
{
    //public static IResourceBuilder<T> ApplyEnvironmentConfiguration<T>(
    //    this IResourceBuilder<T> builder,
    //    AppEnvironment env) where T : IResourceWithEnvironment
    //{
    //    foreach (var (key, val) in env.ToDictionary())
    //        if (!string.IsNullOrWhiteSpace(val))
    //            builder.WithEnvironment(key, val);
    //    return builder;
    //}
}
